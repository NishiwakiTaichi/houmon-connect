<h1 class="fs-4 mb-3">利用者情報を編集 — <%= @client.name %> さん</h1>

<div class="card app-card mb-3">
  <div class="card-body">
    <%= render "form", client: @client %>
  </div>
</div>

<%= render "client_suspensions/list", client: @client %>
