<h1 class="fs-4 mb-3">基本ルート</h1>

<p class="text-muted-app small">
  空き枠(＋)をクリックすると新規登録、既存の訪問をクリックすると編集できます。
</p>

<%= turbo_frame_tag "planning_grid" do %>
  <%= render "planning_section", grid: @planning_grid, service: @service %>
<% end %>
