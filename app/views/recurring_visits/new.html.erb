<h1 class="fs-4 mb-3">基本ルートを登録</h1>

<%= render "planning_grid", grid: @planning_grid %>

<div class="card app-card" id="route-form">
  <div class="card-header fw-bold">登録フォーム</div>
  <div class="card-body">
    <%= render "form", recurring_visit: @recurring_visit %>
  </div>
</div>
