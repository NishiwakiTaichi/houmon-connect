<%# ロゴマーク: 看護(ティール)とリハ(コーラル)の輪がつながるイメージ(モック準拠) %>
<svg width="<%= local_assigns.fetch(:size, 34) %>" height="<%= local_assigns.fetch(:size, 34) %>" viewBox="0 0 40 40" fill="none" aria-hidden="true">
  <circle cx="14" cy="20" r="9.5" stroke="#0e7c72" stroke-width="4.5"/>
  <circle cx="26" cy="20" r="9.5" stroke="#e2725b" stroke-width="4.5"/>
</svg>
